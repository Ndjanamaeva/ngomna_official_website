{
  "version": 2,
  "builds": [
    {
      "src": "front_end/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "installCommand": "npm install",
        "outputDirectory": "front_end/dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "front_end/dist/index.html" }
  ]
}